/**
 * Fixed reference date for deterministic story renders.
 * Used by Chromatic visual regression — change cautiously, baseline must be re-approved.
 */
export const FIXED_DATE = new Date(2016, 1, 5, 14, 30);
